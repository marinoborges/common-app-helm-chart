apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "common-app.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "common-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "common-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
